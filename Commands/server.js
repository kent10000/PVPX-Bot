const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    
    return message.channel.send("Join the PVPX Server, at mc.pvpx.cf! RELEASING JUNE 1 Version 1.8.x-1.16.x")

}

module.exports.help = {
    name: "server"
}